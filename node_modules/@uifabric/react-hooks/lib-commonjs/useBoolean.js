"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var useConstCallback_1 = require("./useConstCallback");
/**
 * Hook to store a value and generate callbacks for setting the value to true or false.
 * The identity of the `setTrue` and `setFalse` callbacks will always stay the same.
 *
 * @param initialState - Initial value
 * @returns Array with the current value and an object containing the updater callbacks.
 */
function useBoolean(initialState) {
    var _a = React.useState(initialState), value = _a[0], setValue = _a[1];
    var setTrue = useConstCallback_1.useConstCallback(function () { return setValue(true); });
    var setFalse = useConstCallback_1.useConstCallback(function () { return setValue(false); });
    return [
        value,
        {
            setTrue: setTrue,
            setFalse: setFalse,
            toggle: value ? setFalse : setTrue,
        },
    ];
}
exports.useBoolean = useBoolean;
//# sourceMappingURL=useBoolean.js.map